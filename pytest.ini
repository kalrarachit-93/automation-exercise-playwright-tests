[pytest]
pythonpath = .
testpaths = tests
markers =
    smoke: critical-path tests that must pass on every push
    slow: tests that register users or complete checkout
    visual: screenshot comparison, environment-sensitive
